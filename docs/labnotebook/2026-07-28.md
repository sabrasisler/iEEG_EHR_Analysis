# 2026-07-28

## 10:30 — Closed three mask-coverage gaps; found a mislabeled flatline exclusion

**Ran:** no analysis — a QC-plumbing day. Audited what `qc/feature_level/`
actually ran (P2.1 metric stage, array `36103871`, 87 tasks, finished 08:19 today,
89 subject-sessions / 12 GB), then chased why several subjects were missing from
masks one level up. Three gaps, all the same shape: **a missing mask never stops
anything here, it silently degrades the result.**

**Output:** `$DERIV/qc/raw_voltage/` (masks rebuilt),
`$DERIV/qc/bipolar/exclusions/bipolar_variance/std10/`,
`$DERIV/qc/feature_level/validation/mask_coverage/<timestamp>/`
**Commit:** `3c7af73` (feature_level threshold+mask stages, previously untracked),
`4175bf5` (`qc/report_mask_coverage.py`)
**Index:** `report_mask_coverage.py` calls `log_analysis()`; the mask/exclusion
rebuilds write `params.json` and are not analyses.

**Saw:**

- **sub-236's only real gap was `square_wave/frac0.9`.** Three of the four
  per-type exclusions the pinned mask needs already existed. That single absence
  is why every `build_mask` has printed "not shared by all types (skipped):
  sub-236" and why exclusion labels sat at 90 subjects while masks sat at 89.
- **sub-236's metrics were complete all along** — not truncated, despite having no
  `metrics/run_info/` record. The registry lists 109 runs, the metrics contain
  107, and both absentees (`run-IA619286`, `run-IA619298`) are among the 6
  registry rows with unparseable `n_channels` — i.e. corrupt NWBs skipped by
  design, the same pattern as sub-085's 83-of-85. So the expensive
  `run_pipeline` pass did **not** need re-running; the missing run_info is a
  provenance gap only.
- **sub-236's flatline exclusion has been mislabeled for weeks.** It sits at
  `exclusions/flatline/logz4_masked-gross-std3_satmargin15_sw/` (Jul 15), but the
  intermediate mask has never contained sub-236, and
  `build_exclusions.py:153-159` falls back to an **unmasked** baseline with only a
  warning. So for that one subject the label claims `masked-…` while the baseline
  was not. Rebuilt as stage 3 of today's chain, after the intermediate mask
  finally includes it.
- **The bipolar `std10` gap was sequencing, not failure.** The std10 run was
  Jul 23 14:47–15:04; its subject list required "has a bipolar_variance metric AND
  a mask at this label"; the five OOM-retry subjects' masks were only written
  Jul 27 16:55–16:57, four days later. They were correctly ineligible at the time.
- **The whole pinned mask was rebuilt Jul 27 16:42–16:57**, all 89 files, so the
  82 std10 exclusions derived from it (Jul 23) are now *older than their input*.
  Accepted as provenance-only rather than rebuilt: `build_pinned_masks.sbatch`
  md5-fingerprints before/after and reports any pre-existing file that changed,
  which is the check that would catch it if the assumption is wrong.
- **The "no mask" failure is directionally sneaky.** An unmasked baseline keeps
  artifact windows → inflates the std → *deflates* z → the detector becomes **less
  sensitive** for exactly the subjects whose QC inputs were incomplete. A
  cross-subject comparison of flag rates would read that as "cleaner".
- **Tier 2 of the new report is only sound because the mask CSVs are dense** (they
  carry `excluded=False` rows), so a run absent from a mask file means absent
  coverage rather than a clean run. Recorded as a precondition in the module
  docstring — if the mask tree ever goes sparse, that check breaks.

**Broken / needs rerun:**

- Stage 1 of the sub-236 chain recorded `dirty: true` in three `params.json`
  files, because `report_mask_coverage.py` was still untracked when the job
  launched. Cosmetic — the dirty file is unrelated to `build_exclusions` — but it
  is the same class of provenance blemish the commit-before-you-run rule exists to
  prevent, and it was self-inflicted by submitting before committing.
- `src/ieeg_ehr/features/dtype_audit.py` has an uncommitted refactor (80+/139−,
  dropping the cross-module private import of `build_pain_epoch_power`). Left
  alone rather than bundled into today's commits; it needs its own.

**Spawned:**
- task → `TASKS.md`: make a missing mask impossible to miss (3 silent fallback
  sites; stop a label claiming a mask it did not use; record per-run coverage)
- task → `TASKS.md`: revisit `_EXCLUDE_FROM_EXPLORATORY = {'236'}` now that
  sub-236 has a real mask
- ticked → `TASKS.md`: the 5 OOM-retry subjects were already rolled into the
  pinned mask on Jul 27, just never checked off

**Refs:** PLANNING P2.1 · P0.1 · notebook 2026-07-27 · `docs/qc_context.md`
("sub-236 gap", the mask-globbing bug) · SCRATCHPAD (`build_bipolar_exclusions`
label/clobber gotcha)
