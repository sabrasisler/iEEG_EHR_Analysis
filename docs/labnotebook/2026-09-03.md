# 2026-09-03

## 14:53 — Built `med_analysis`; ran the four analgesic administration figures

**Ran:** Adapted a colleague's benzodiazepine MAR analysis
(`/home/groups/ckeller1/sisler/iEEG-EHR_Code/med_admin/`) to analgesics, as a new
subpackage `src/ieeg_ehr/med_analysis/` writing to a new level-1 event `meds` on
Oak. Four figures plus the tidy administration table they all render from. All
five ran in one job (`sbatch/med_figures.sbatch`, ~25 s total).

**Output:** `$DERIV/analysis/meds/administration_patterns/{admin_table,burden_scatter,timing_hist,hospital_day,coadmin_peth}/*_20260903-1453*/`
**Commit:** `13d3e67cc3ff` +dirty <!-- dirty = unrelated in-progress pain_coef/dtype_audit files; the med_analysis code itself is committed -->
**Index:** logged in `docs/analyses_run.md`

**Saw:**

- The loader reproduces the source analysis's independently published corpus
  totals *exactly* — 98 files, 7,340 MAR rows, 421 multi-product rows collapsed,
  6,919 unique administrations, 380 benzodiazepines, 0 unmatched drug names. That
  validates name cleaning, the dedup key and the taxonomy join in one shot against
  numbers this code did not produce. Pinned as a test.
- 1,754 analgesic administrations in 93 subjects: opioids 1,062, acetaminophen
  643, NSAIDs 49.
- **Anesthetics are effectively absent from the export.** Propofol n=1, rocuronium
  n=3, lidocaine n=21 (mostly topical), no ketamine / dexmedetomidine /
  remifentanil, and not one row with a populated `infusion_rate` across all 98
  sessions. The MAR export does not capture procedural medication. Dropped them
  from the drug set (DECISIONS 2026-09-03).
- notable: `timing_hist/*/fig2b_interdose_interval.png` — narrowing the interval
  axis from the source's 0–30 h to 0–24 h was the right call; opioid inter-dose
  intervals pile up at 4–6 h across every formulation, which was invisible spread
  over 30 h.
- notable: `hospital_day/*/fig3_hospital_day_grid.png` — administration rate
  tapers across days 0–6 for the opioids (hydrocodone-acetaminophen 2.6 → 0.7 per
  24 recorded hours, oxycodone 2.6 → 1.0) while acetaminophen stays flat at
  ~1.5. The normalized-dose row sits near 1.0 throughout for everything. Reads as
  a taper in *frequency* rather than in dose *magnitude* — nomination, not a
  finding, and it rests on a denominator that is only good to a few percent (see
  below).
- notable: `coadmin_peth/*/fig4_coadmin_peth.png` — anticonvulsant zero-bin peak
  under every analgesic row (same medication pass). In the opioid column against
  opioid rows there is a zero-bin *dip* with shoulders at ±4 h, which is the
  anchor-exclusion plus q4h redosing. IV fentanyl shows a benzodiazepine zero-bin
  peak (~0.17) that nothing else does.
- Time-of-day at the source's finer binning was noise for the low-n panels;
  moved the default to 60-min bins. Acetaminophen then shows clean medication-pass
  peaks.

**Broken / needs rerun:** The recorded-hours denominator is weaker than expected.
`sherlock_file_registry.csv` only populates `start_datetime` for runs that have a
PREPROCESSED file — all 2,136 null-timestamp rows have `has_preprocessed ==
False` — so registry timing measures preprocessed coverage, not recorded coverage.
Of the 98 med sessions, 41 are fully timestamped, 41 partial, 16 have none, and an
untimestamped run cannot be placed on a hospital day at all. Currently falling back
to MAR `session_start`→`session_end` span for 17 of the 95 analgesic sessions
(11,853 h used vs 12,759 h of span). Where the registry is complete, coverage is a
median 0.965 of span. Fig 3's rates are therefore good to a few percent, not exact.

**Spawned:**
- task → `TASKS.md`: build a raw-NWB span manifest (per-subject array over
  `session_start_time` + `starting_time` + `rate` + data shape, all confirmed
  present and metadata-only) so recorded hours means recorded, and Fig 3 uses one
  method for every session.

**Refs:** PLANNING level-1 event `meds` · DECISIONS 2026-09-03 (six calls: drug
set, dose units, day-0 anchor, cohort, denominator, CSV-under-`analysis/`)

---

## 16:16 — Fig 1 relabelled: every drug named, not just the top six

**Ran:** `plot_admin_burden` only (`FIGURES="burden"`), after replacing the
fixed-offset labelling with a placer. `label_top=6` had left half the panel
anonymous — the five low-n opioids (tramadol, morphine, buprenorphine-naloxone,
acetaminophen-codeine, oxycodone-acetaminophen) and ibuprofen had no names — so
the one question this figure exists to answer, which drugs can carry Figs 2-4,
could only be answered by joining markers to `drug_summary.csv` by eye. The
alternating above/below offsets also made the pairing ambiguous for the six that
WERE labelled: "Ketorolac" sat nearer tramadol's marker than ketorolac's, and
"Hydrocodone-Acetaminophen" ran off the right edge.

**Output:** `$DERIV/analysis/meds/administration_patterns/burden_scatter/burden_scatter_20260903-161614/`
**Commit:** `151200a884b9` (clean — this run's code was committed before it ran)
**Index:** logged in `docs/analyses_run.md`

**Saw:**

- All 12 drugs now carry a name, with leader lines only where a label had to
  travel far enough that its marker would otherwise be a guess. Nothing overlaps
  and nothing is clipped.
- Numbers are byte-identical to the 14:53 run — same 12 drugs, 1,754
  administrations, n=93, same corpus totals. This was a rendering change only,
  and the 14:53 run is left in place rather than replaced.
- Overflowing the axes is scored as a soft cost rather than vetoed, which keeps
  the panel from stretching sideways to accommodate the two longest names; they
  now sit inside the plot area. Worth knowing the escape hatch exists, because a
  denser panel will eventually need it.

**Refs:** `style.label_points` · DECISIONS 2026-09-03 (unchanged — no analytic
call moved) · notebook 14:53 above for the original four-figure run
