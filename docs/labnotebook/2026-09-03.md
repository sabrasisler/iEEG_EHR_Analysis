# 2026-09-03

## 14:53 — Built `med_analysis`; ran the four analgesic administration figures

**Ran:** Adapted a colleague's benzodiazepine MAR analysis
(`/home/groups/ckeller1/sisler/iEEG-EHR_Code/med_admin/`) to analgesics, as a new
subpackage `src/ieeg_ehr/med_analysis/` writing to a new level-1 event `meds` on
Oak. Four figures plus the tidy administration table they all render from. All
five ran in one job (`sbatch/med_figures.sbatch`, ~25 s total).

**Output:** `$DERIV/analysis/meds/administration_patterns/{admin_table,burden_scatter,timing_hist,hospital_day,coadmin_peth}/*_20260903-1453*/`
**Commit:** `13d3e67cc3ff` +dirty <!-- dirty = unrelated in-progress pain_coef/dtype_audit files; the med_analysis code itself is committed -->
**Index:** logged in `docs/analyses_run.md`

**Saw:**

- The loader reproduces the source analysis's independently published corpus
  totals *exactly* — 98 files, 7,340 MAR rows, 421 multi-product rows collapsed,
  6,919 unique administrations, 380 benzodiazepines, 0 unmatched drug names. That
  validates name cleaning, the dedup key and the taxonomy join in one shot against
  numbers this code did not produce. Pinned as a test.
- 1,754 analgesic administrations in 93 subjects: opioids 1,062, acetaminophen
  643, NSAIDs 49.
- **Anesthetics are effectively absent from the export.** Propofol n=1, rocuronium
  n=3, lidocaine n=21 (mostly topical), no ketamine / dexmedetomidine /
  remifentanil, and not one row with a populated `infusion_rate` across all 98
  sessions. The MAR export does not capture procedural medication. Dropped them
  from the drug set (DECISIONS 2026-09-03).
- notable: `timing_hist/*/fig2b_interdose_interval.png` — narrowing the interval
  axis from the source's 0–30 h to 0–24 h was the right call; opioid inter-dose
  intervals pile up at 4–6 h across every formulation, which was invisible spread
  over 30 h.
- notable: `hospital_day/*/fig3_hospital_day_grid.png` — administration rate
  tapers across days 0–6 for the opioids (hydrocodone-acetaminophen 2.6 → 0.7 per
  24 recorded hours, oxycodone 2.6 → 1.0) while acetaminophen stays flat at
  ~1.5. The normalized-dose row sits near 1.0 throughout for everything. Reads as
  a taper in *frequency* rather than in dose *magnitude* — nomination, not a
  finding, and it rests on a denominator that is only good to a few percent (see
  below).
- notable: `coadmin_peth/*/fig4_coadmin_peth.png` — anticonvulsant zero-bin peak
  under every analgesic row (same medication pass). In the opioid column against
  opioid rows there is a zero-bin *dip* with shoulders at ±4 h, which is the
  anchor-exclusion plus q4h redosing. IV fentanyl shows a benzodiazepine zero-bin
  peak (~0.17) that nothing else does.
- Time-of-day at the source's finer binning was noise for the low-n panels;
  moved the default to 60-min bins. Acetaminophen then shows clean medication-pass
  peaks.

**Broken / needs rerun:** The recorded-hours denominator is weaker than expected.
`sherlock_file_registry.csv` only populates `start_datetime` for runs that have a
PREPROCESSED file — all 2,136 null-timestamp rows have `has_preprocessed ==
False` — so registry timing measures preprocessed coverage, not recorded coverage.
Of the 98 med sessions, 41 are fully timestamped, 41 partial, 16 have none, and an
untimestamped run cannot be placed on a hospital day at all. Currently falling back
to MAR `session_start`→`session_end` span for 17 of the 95 analgesic sessions
(11,853 h used vs 12,759 h of span). Where the registry is complete, coverage is a
median 0.965 of span. Fig 3's rates are therefore good to a few percent, not exact.

**Spawned:**
- task → `TASKS.md`: build a raw-NWB span manifest (per-subject array over
  `session_start_time` + `starting_time` + `rate` + data shape, all confirmed
  present and metadata-only) so recorded hours means recorded, and Fig 3 uses one
  method for every session.

**Refs:** PLANNING level-1 event `meds` · DECISIONS 2026-09-03 (six calls: drug
set, dose units, day-0 anchor, cohort, denominator, CSV-under-`analysis/`)

---

## 16:16 — Fig 1 relabelled: every drug named, not just the top six

**Ran:** `plot_admin_burden` only (`FIGURES="burden"`), after replacing the
fixed-offset labelling with a placer. `label_top=6` had left half the panel
anonymous — the five low-n opioids (tramadol, morphine, buprenorphine-naloxone,
acetaminophen-codeine, oxycodone-acetaminophen) and ibuprofen had no names — so
the one question this figure exists to answer, which drugs can carry Figs 2-4,
could only be answered by joining markers to `drug_summary.csv` by eye. The
alternating above/below offsets also made the pairing ambiguous for the six that
WERE labelled: "Ketorolac" sat nearer tramadol's marker than ketorolac's, and
"Hydrocodone-Acetaminophen" ran off the right edge.

**Output:** `$DERIV/analysis/meds/administration_patterns/burden_scatter/burden_scatter_20260903-161614/`
**Commit:** `151200a884b9` (clean — this run's code was committed before it ran)
**Index:** logged in `docs/analyses_run.md`

**Saw:**

- All 12 drugs now carry a name, with leader lines only where a label had to
  travel far enough that its marker would otherwise be a guess. Nothing overlaps
  and nothing is clipped.
- Numbers are byte-identical to the 14:53 run — same 12 drugs, 1,754
  administrations, n=93, same corpus totals. This was a rendering change only,
  and the 14:53 run is left in place rather than replaced.
- Overflowing the axes is scored as a soft cost rather than vetoed, which keeps
  the panel from stretching sideways to accommodate the two longest names; they
  now sit inside the plot area. Worth knowing the escape hatch exists, because a
  denser panel will eventually need it.

**Refs:** `style.label_points` · DECISIONS 2026-09-03 (unchanged — no analytic
call moved) · notebook 14:53 above for the original four-figure run

---

## 16:31 — Fig 1 axes: linear instead of symlog

**Ran:** `plot_admin_burden` again, after replacing the symlog axes with linear
ones. The symlog was unreadable two ways: `linthresh=10` meant position said one
thing below 10 administrations and another above it, and a single shared limit
driven by x's maximum (643) left everything above y=73 as empty panel, since no
drug can have more subjects than administrations. Only 10 and 100 were ever
labelled, so no value could be read off either axis.

**Output:** `$DERIV/analysis/meds/administration_patterns/burden_scatter/burden_scatter_20260903-163138/`
**Commit:** `4356f43496ee` +dirty <!-- dirty = the same unrelated in-flight pain_coef/dtype_audit files as the 14:53 run; the med_analysis code is exactly at this commit -->
**Index:** logged in `docs/analyses_run.md`

**Saw:**

- Ticked every 100 administrations and every 10 subjects with a minor grid, so
  values are now readable directly: acetaminophen 643/73, hydrocodone-
  acetaminophen 493/61, oxycodone 216/26, hydromorphone 157/15, fentanyl 94/27.
- **The cost, and it is real:** 7 of the 12 drugs sit under 50 administrations
  and now crowd the leftmost tenth of the x axis, where their positions are no
  longer separable. Those drugs are carried by their labels and by
  `drug_summary.csv`, not by their coordinates. Acceptable because the panel's
  job is to pick the drugs that can carry Figs 2-4, and every drug it excludes
  is excluded for being in exactly that corner — but if the low-n end ever
  needs to be read, this wants an inset rather than a scale change.
- What linear buys: a distance means the same thing everywhere, so ratios among
  the drugs that DO carry Figs 2-4 read straight off the axis.
- `label_points` needed widening to survive the cluster — far rings to 140pt,
  finer angle steps, and an opaque backing behind the text, since in a cluster
  some other point's leader line crosses a label however well it is placed.
  Without the far rings every label in the corner fell back to least-overlap and
  they printed on top of each other.
- Numbers unchanged again: 12 drugs, 1,754 administrations, n=93.

**Refs:** notebook 16:16 above (the relabelling that preceded this) ·
`style.label_points`

---

## 16:59 — Fig 5: where on the pain scale each analgesic is actually used

**Ran:** New level-2 question `pain_coupling`. Each administration of the four
most-administered analgesics joined to the pain score charted in the 30 min
before it (`merge_asof`, backward, `by=[subject, session]`), then binned:
x = that score 0-10, y = administrations, one bar per drug.

**Output:** `$DERIV/analysis/meds/pain_coupling/pain_score_bars/pain_score_bars_20260903-165936/`
**Commit:** `7849145f87bf` +dirty <!-- dirty = the same unrelated in-flight pain_coef/dtype_audit files as the earlier runs; the med_analysis code is exactly at this commit -->
**Index:** logged in `docs/analyses_run.md`

**Saw:**

- **A clean potency ladder in the median preceding score:** acetaminophen 4
  (IQR 3-6), hydrocodone-acetaminophen 6 (5-7), oxycodone 7 (5-8),
  hydromorphone 8 (7-9). Share of administrations given at a score of 7+ runs
  23% / 50% / 53% / 77% across the same four. Nobody chose this ordering — it
  falls out of the join, and it is the first thing in this dataset that looks
  like dosing tracking reported pain rather than the clock.
- Acetaminophen is the only one of the four with meaningful low-score use: 5.7%
  of its administrations follow a score of 0, versus 0.5-1.0% for the three
  opioids. It peaks at score 3; the opioids peak at 7 (hydrocodone-
  acetaminophen, oxycodone) and 8 (hydromorphone).
- Score 1 is nearly empty across all four drugs (8 administrations total, all
  acetaminophen) — the scale is used coarsely in practice, which is why
  `counts_by_score` reindexes the full 0-10 range rather than letting the gap
  silently close.
- 1,113 of 1,509 administrations linked (74%); 396 dropped for having no
  assessment in the 30 min before the dose. Median gap is 0.0 min for all four
  drugs, i.e. the modal case is a score and a dose charted in the same minute.
- **Reads against Fig 3.** Fig 3's normalized-dose row sat near 1.0 flat across
  hospital days while administration RATE tapered. Fig 5 says the same thing
  from the other side: which drug gets reached for tracks the score, and the
  taper is in frequency. Consistent, not redundant.

**Broken / needs care:** Fig 5's totals do NOT reconcile with Fig 1's, by
design — the 30-min window is an inclusion criterion, so 26% of administrations
are absent. The excluded count is printed on the figure for that reason. And
the 45% same-minute tie rate is the load-bearing assumption of the whole panel:
it is defensible from charting workflow and the shape is unchanged without it,
but the data cannot prove ordering within a minute (DECISIONS 2026-09-03, call 2).

**Spawned:** nothing to `TASKS.md` — the obvious next step (does the *dose*, not
just the choice of drug, scale with the preceding score?) is a question, not a
task, and the per-drug unit problem from DECISIONS call 2 of the earlier entry
applies to it.

**Refs:** PLANNING level-1 event `meds`, question `pain_coupling` ·
DECISIONS 2026-09-03 "Linking a dose to the pain score before it" ·
notebook 14:53 above for Figs 1-4

---

## 17:10 — Fig 5c (violin) and Fig 6 (doses given), and what the doses reveal

**Ran:** Re-ran Fig 5 with a third panel added, and a new figure for dose.

**Output:**
- `$DERIV/analysis/meds/pain_coupling/pain_score_bars/pain_score_bars_20260903-171033/` (5a, 5b, 5c)
- `$DERIV/analysis/meds/administration_patterns/dose_distribution/dose_distribution_20260903-171036/` (fig 6)

**Commit:** `190c25dda0a1` +dirty <!-- dirty = the same unrelated in-flight pain_coef/dtype_audit files as every run today -->
**Index:** logged in `docs/analyses_run.md`

**Saw:**

- **Fig 5c** puts the ladder in one panel: medians 4 / 6 / 7 / 8 for
  acetaminophen / hydrocodone-acetaminophen / oxycodone / hydromorphone, IQRs
  3-6 / 5-7 / 5-8 / 7-9. Overplotting the individual administrations was worth
  it — they land in eleven rows and make it obvious the scale is used coarsely,
  which the smooth violin outline would otherwise hide (it is a KDE of integers).
- **Fig 6: dosing is close to bimodal-by-habit, not continuous.** Acetaminophen
  is 650 mg in 493 of 643 administrations; hydrocodone-acetaminophen is 1 tablet
  in 429 of 493; oxycodone splits 5/10/15 mg fairly evenly (86/76/53).
  Hydromorphone is the only one with real spread, and that spread is a ROUTE
  split, not a dosing decision: IV 0.2-1 mg, oral 1-4 mg. An unsplit histogram
  of it would have shown a spurious bimodal "dose choice".
- Zero unparseable doses across all four drugs, and each is single-unit, so
  `assert_single_unit` passed rather than being load-bearing here. It stays as
  the guard for the next drug set.
- **Several dose bars are one patient**, which the counts hide and `n_subjects`
  does not: hydromorphone 1.0 mg IV is 40 administrations from 5 subjects,
  0.4 mg is 6 from 1 subject, acetaminophen 975 mg is 13 from 1 subject, and
  oxycodone's feeding-tube rows are 1 subject each. Any statement about "which
  dose is typical" has to read the n_subjects column of `dose_counts.csv`, not
  the bar height. This is the same fragility Fig 1 exists to flag, one level down.
- The obvious next question is whether the DOSE scales with the preceding score
  within a drug, not just which drug is chosen. That is answerable from
  `linked_administrations.csv` (it carries dose, unit and pain_score per row)
  and needs no new extraction — but it is per-drug and per-route by necessity,
  since hydromorphone IV and oral are different scales.

**Refs:** DECISIONS 2026-09-03 "Linking a dose to the pain score before it" ·
notebook 16:59 above · PLANNING question `pain_coupling`

---

## 17:36 — Figs 2, 3, 5 and 6 re-run on one seven-drug set

**Ran:** All four re-run with the drug set named explicitly rather than each
figure re-deriving it from `value_counts` + `--min-admin` + its own panel cap.
Set: acetaminophen, hydrocodone-acetaminophen, oxycodone, hydromorphone,
fentanyl, ketorolac, tramadol.

**Output:**
- `$DERIV/analysis/meds/administration_patterns/timing_hist/timing_hist_20260903-173647/` (2a, 2b)
- `$DERIV/analysis/meds/administration_patterns/hospital_day/hospital_day_20260903-173652/` (3, 3b)
- `$DERIV/analysis/meds/pain_coupling/pain_score_bars/pain_score_bars_20260903-173656/` (5a, 5b, 5c)
- `$DERIV/analysis/meds/administration_patterns/dose_distribution/dose_distribution_20260903-173659/` (6)

**Commit:** `da927fb2b8e6` +dirty <!-- dirty = the same unrelated in-flight pain_coef/dtype_audit files as every run today -->
**Index:** logged in `docs/analyses_run.md`

**Saw:**

- **The pain ladder survives adding non-opioids and gets more interpretable.**
  Median preceding score / fraction given at 7+: ketorolac 4 / 13%,
  acetaminophen 4 / 23%, tramadol 5 / 13%, hydrocodone-acetaminophen 6 / 50%,
  oxycodone 7 / 53%, fentanyl 7 / 58%, hydromorphone 8 / 77%. The two
  non-opioids sit at the bottom with acetaminophen and the potent opioids at
  the top, which is the ordering a clinician would predict — nobody chose it,
  it falls out of the join.
- **Ketorolac is the exception worth noting:** 22% of its administrations follow
  a score of 0, an order of magnitude more than any opioid (0.5-1%) and 4x
  acetaminophen. Its IQR starts at 1.5. Reads like scheduled/anti-inflammatory
  use rather than pain-triggered — but n=23 from 6 subjects, so it is a hint.
- **Fentanyl is peri-procedural, and Fig 3 shows it plainly:** 2.3
  administrations per 24 recorded hours on day 0, collapsing to ~0.25 by day 2.
  Ketorolac front-loads the same way (2.1 on days 0-1). The oral opioids taper
  gradually instead; acetaminophen stays flat at ~1.6 all week.
- Fig 2a: acetaminophen has clean medication-pass peaks (6a / 12p / 6p / 12a);
  fentanyl is diffuse and daytime-weighted, consistent with as-needed IV use.
  Fig 2b: PO tramadol peaks at 8-9 h and IV ketorolac at 6 h, both consistent
  with scheduled dosing, while IV fentanyl is short and long-tailed.
- Fig 6: fentanyl is 50 mcg in 44 of 94 and 25 mcg in 33; ketorolac 15 mg in 37
  of 47; tramadol 50 mg in 35 of 39. Single-mode by habit, like the others.
- **Fragility to carry forward:** hydromorphone (14 subjects), oxycodone (25),
  fentanyl (14), tramadol (9) and ketorolac (6) all rest on few patients, and
  Fig 3's normalized-dose row for ketorolac and tramadol swings on 1-3
  contributing subjects per day. The per-day subject counts are annotated on
  the bars for that reason. Read `n_subjects`, not bar height.

**Broken / needs rerun:** nothing. Four defects were found by rendering at seven
drugs and fixed in `da927fb` before these runs: an empty fig2b panel for IV
acetaminophen (a formulation can clear the administration threshold and still
have no inter-dose interval), "from 1 subjects", an axis-label/footnote
collision in fig5a/5b, and a fig6 panel title overflowing into its neighbour.

**Spawned:** nothing to `TASKS.md`. The ketorolac zero-pain observation is a
question, not a task — it belongs in `SCRATCHPAD.md` if it survives a second
look, and per CLAUDE.md a sweep result is a nomination, not a finding.

**Refs:** DECISIONS 2026-09-03 "Linking a dose to the pain score before it" ·
notebook 16:59 and 17:10 above · `load.select_drugs`
